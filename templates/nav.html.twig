<nav class="navbar">
	<div class="navbar-left">
		<a href="{{ path('app_home') }}" class="navbar-brand">
			<img src=" {{ asset('img/logo.png') }} " alt=" Logo Battle Assistant" class="navbar-logo">
		</a>
	</div>

	<div class="navbar-middle">
		<h1 class="navbar-title">BATTLE ASSISTANT</h1>
		<div class="navbar-raw-1">
			<a href="{{ path('app_scenario_index') }}" class="navbar-link-raw-1">Scenario</a>
			<a href="{{ path('app_rule_index') }}" class="navbar-link-raw-1">Rule</a>
		</div>
		{% if is_granted('ROLE_ADMIN') %}
			<div class="navbar-raw-2">
				<a href="{{ path('app_battlefield_index') }}" class="navbar-link-raw-2">Battlefield</a>
				<a href="{{ path('app_component_index') }}" class="navbar-link-raw-2">Component</a>
				<a href="{{ path('app_event_index') }}" class="navbar-link-raw-2">Event</a>
				<a href="{{ path('app_expansion_pack_index') }}" class="navbar-link-raw-2">ExpansionPack</a>
				<a href="{{ path('app_rule_index') }}" class="navbar-link-raw-2">Rule</a>
				<a href="{{ path('app_scenario_index') }}" class="navbar-link-raw-2">Scenario</a>
				<a href="{{ path('app_user_index') }}" class="navbar-link-raw-2">User</a>
			</div>
		{% endif %}
	</div>
	<!-- 
																																														<div class="navbar-xxxxx">
																																															{# {% if is_granted('ROLE_USER') %} #}
																																																		{# <a href="{{ path('app_book_index') }}" class="navbar-link">Calendrier</a> #}
																																																		{# <a href="{{ path('app_reservation_index') }}" class="navbar-link">Reservations</a> #}
																																																{# {% endif %} #}
																																																{# {% if is_granted('ROLE_ADMIN') %} #}
																																																	{# <a href="{{ path('app_user_index') }}" class="navbar-link">Utilisateurs</a> #}
																																															{# {% endif %} #}
																																															</div>
																															-->

	<div class="navbar-right">
		{% if is_granted('ROLE_USER') %}
			<div class="navbar-user">
				<a href="#" class="navbar-username">{{ app.user.username }}</a>
				<p class="navbar-roles">{{ app.user.roles[0] }}</p>
			</div>
			<a href="{{ path('app_logout') }}" class="navbar-link-right">Se déconnecter</a>
		{% else %}
			<a href="{{ path('app_login') }}" class="navbar-link-right">Se connecter</a>
			<a href="{{ path('app_register') }}" class="navbar-link-right">S'enregistrer</a>
		{% endif %}

	</div>
</nav>
