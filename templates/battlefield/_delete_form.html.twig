<form method="post" action="{{ path('app_battlefield_delete', {'id': battlefield.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ battlefield.id) }}">
    <button class="btn">Delete</button>
</form>
