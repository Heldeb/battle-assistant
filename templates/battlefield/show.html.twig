{% extends 'base.html.twig' %}

{% block title %}Battlefield{% endblock %}

{% block body %}
    <h1>Battlefield</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ battlefield.id }}</td>
            </tr>
            <tr>
                <th>Battlefield_type</th>
                <td>{{ battlefield.battlefieldType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_battlefield_index') }}">back to list</a>

    <a href="{{ path('app_battlefield_edit', {'id': battlefield.id}) }}">edit</a>

    {{ include('battlefield/_delete_form.html.twig') }}
{% endblock %}
