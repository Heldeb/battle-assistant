{% extends 'base.html.twig' %}

{% block title %}Scenario{% endblock %}

{% block body %}
    <h1>Scenario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scenario.id }}</td>
            </tr>
            <tr>
                <th>Scenario_name</th>
                <td>{{ scenario.scenarioName }}</td>
            </tr>
            <tr>
                <th>Medal_count</th>
                <td>{{ scenario.medalCount }}</td>
            </tr>
            <tr>
                <th>Historical_description</th>
                <td>{{ scenario.historicalDescription }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_scenario_index') }}">back to list</a>

    <a href="{{ path('app_scenario_edit', {'id': scenario.id}) }}">edit</a>

    {{ include('scenario/_delete_form.html.twig') }}
{% endblock %}
